apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: expense-platform

resources:
  - deployments.yaml

commonLabels:
  app.kubernetes.io/part-of: expense-intelligence-platform
  app.kubernetes.io/managed-by: kustomize
